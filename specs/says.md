# says — 플라밍고 세이즈 (원작: Clefairy Says, Pokémon Stadium 1 / N64 Kids Club)

> 이 문서만 보고 `games/says.html`을 다시 만들 수 있어야 한다. 확정 사실은 [확정], 출처가 침묵한 부분은 "불명 — 추정: …"으로 표기.

## 1. 한 줄 요약 · 등장 캐릭터와 플라밍고 대체안

**한 줄 요약**: 교실에서 선생님이 칠판에 그리는 화살표 순서(4→6→8→10→12개)를 외웠다가 D패드로 똑같이 입력하는 4인 동시 기억력(Simon Says) 게임. 틀리거나 입력을 안 하면 선생님이 장난감 망치로 때리고(HP −1), 5대 맞으면 탈락. 5라운드 뒤 가장 적게 맞은 쪽이 승리. [확정] 원작 9개 미니게임 중 "가장 어려운 게임"으로 평가됨.

| 원작 요소 | 원작 묘사 | 플라밍고 대체안 (셸 포즈 활용) |
|---|---|---|
| 학생 삐삐(Clefairy) ×4 | 책상 앞에 서서 노래에 맞춰 춤추는 분홍 삐삐, 플레이어 색 리본/번호 | `drawFlamingo` 플레이어 색. 대기 `idle`, 입력 시 방향에 맞춰 `jump`(↑) / `dig`(↓, 고개 숙임) / `run` 좌우 `flip`(←→). 정답 라운드 완료 `fly` 잠깐. 망치 맞을 때 `hit`→`dizzy` 0.8s. HP 0 탈락은 `sleep`(주저앉음). |
| 선생님 삐삐 | 칠판 옆에 선 큰 삐삐, 지시봉으로 화살표를 가리키며 "Clefairy! Clefairy!" 노래. 오답자에게 걸어가 망치질 | 큰 플라밍고(scale 1.6, 금색 `palette.gold`), 안경·지시봉 소품(선분+원). 보여주기 중 `idle`+지시봉 회전, 채점 때 `hit` 모션으로 망치(갈색 사각+손잡이) 휘두름. |
| 칠판 + 화살표 | 초록 칠판에 흰 분필 화살표가 한 개씩 그려짐. 보여주기 후 지우개로 싹 지움 | 상단 중앙 칠판(rr, `#2f5d3a`), 화살표는 흰색 굵은 글리프 `↑↓←→`. 지우기는 0.4s 동안 알파 페이드. |
| 장난감 망치 | 노란 머리 + 빨간 손잡이 삐삐용 뿅망치 | 동일 형태 소품, 맞을 때 `sfx.hit` + 별 파티클. |
| 피격 마크(HP) | 각 삐삐 **머리 위**에 오답 표시가 쌓임, 5개면 탈락 | 머리 위 ○○○○○ → 맞을 때마다 빨간 ✕(`palette.red`)로 채움. |

## 2. 인원 · 시간 · 라운드 구조

- **4인 동시** [확정]. 사람 1~4명 + 나머지 COM. 모두 같은 순서를 같은 타이밍에 본다(개별 순서 아님).
- **제한 시간 없음, 라운드제** [확정]: 총 **5라운드**. 화살표 개수 **4, 6, 8, 10, 12** [확정: "four, four+two, six+two, eight+two, ten+two"].
- **종료 조건** [확정]: (a) 살아남은 플레이어가 1명 이하가 되면 즉시 종료, (b) 5라운드가 끝나면 종료.
- 라운드 1개의 단계 (시간은 불명 — 추정, 원작 영상 체감 기준):
  1. `intro` 0.8s: "라운드 N" 자막, 선생님이 칠판을 가리킴.
  2. `show`: 화살표를 **한 개씩** 그리며 노래. 개당 표시 간격 **0.6s** (라운드 1) → 라운드마다 0.05s 단축, 최소 0.4s. 그려진 화살표는 칠판에 **누적**되어 남는다(끝까지 전부 보임). 원작도 누적 표시라 "종이에 적어 치팅 가능"하다는 언급이 있음 [확정].
  3. `erase` 0.6s: 칠판을 지움. "따라 하세요!" 자막.
  4. `input`: 플레이어가 자유 타이밍으로 순서를 입력. 제한 시간 = **1.0s × 화살표 수 + 2s** (라운드 1: 6s, 라운드 5: 14s) — 불명, 추정. 화면에 타임 바 표시.
  5. `judge` 약 0.6s × (실수한 사람 수) + 0.8s: 선생님이 오답자 앞으로 가서 망치질. 정답자는 `fly` 환호.
  6. 다음 라운드 or 결과.
- 총 소요 약 60~90s.

## 3. 조작

| N64 | 원작 기능 | 우리 액션 |
|---|---|---|
| Control Pad ↑↓←→ | 화살표 순서 입력 [확정: "Control Pad to repeat the pattern"] | `up down left right` (`g.p(i).hit(...)`, 한 프레임 눌림) |
| Control Stick | 불명(모든 출처가 "Control Pad/D-Pad"만 언급) — 추정: 미사용 | `axis()`는 쓰지 않음(오입력 방지). 셸 키 매핑이 `up down left right` 액션으로 이미 통일돼 있으므로 문제 없음. |
| A / B / C | 사용 안 함 | `a`, `b` 무시. |

- **연타/홀드 없음**, 한 번 눌림 = 화살표 1개. 홀드해도 1개만 인식(`hit` 사용).
- **입력 반응 규칙**:
  - `input` 단계에서만 입력 수집. `show`/`erase` 중 입력은 무시(선입력 불가 — 불명, 추정).
  - 같은 프레임에 두 방향 동시 → 먼저 정의된 순서(up,down,left,right) 중 하나만 채택.
  - 입력마다 그 플레이어의 플라밍고가 해당 방향 포즈로 0.25s 춤추고 `sfx.beep`(방향별 음: ↑880 ↓440 ←660 →550Hz, 0.08s).
  - 각 플레이어는 **정답 개수만큼 입력하면 완료**(`done`). 완료 후 추가 입력 무시.
  - 오답을 눌러도 즉시 탈락하지 않고 입력은 계속 받는다(채점은 라운드 끝). 화면에 즉시 X 표시는 하지 않는다(원작은 입력 단계에서 정오 피드백을 주지 않음 — 불명, 추정).
  - 시간 종료 시 미완료자는 "입력 안 함"으로 처리.
  - 생존자 전원이 `done`이면 타임 바가 남았어도 **0.8s 뒤 입력 단계 종료**(원작은 전원 완료 시 바로 채점으로 넘어가는 체감 — 불명, 추정. 빈 대기 시간을 없애 리듬 유지).

## 4. 규칙과 점수

- **점수 단위 = HP(하트) 5개** [확정: 5번 맞으면 탈락]. 내부 점수는 `misses`(누적 피격 수, 0~5).
- **감산 조건(망치 1대 = HP −1)** [확정]: 라운드에서 (a) 순서 중 하나라도 틀림, (b) 아무것도 입력 안 함 / 시간 내 미완료. 라운드당 최대 1대 (불명 — 추정: 여러 개 틀려도 1대).
- **가산 없음**. 정답은 HP 유지.
- **탈락**: misses == 5 → 즉시 탈락, 이후 라운드 참가 불가(교실 뒤에서 `sleep`).
- **승리 판정** [확정]: 최후 생존자 1명이면 그 사람 승리. 5라운드 종료 시 misses 최소 승리.
- **동점** [추정 — 2차 검증에서 GamesRadar 원문은 "beat the CPU 5 times in a row"라고만 적혀 있고 "or tie" 문구는 확인되지 않음. Serebii의 해금 조건 "5 games in a row with no losses"(패배만 없으면 됨)가 무승부 존재를 간접 시사할 뿐] → 동일 misses는 **공동 순위**, 추가 타이브레이크 없음(출처 어디에도 타이브레이크 규칙이 없으므로 임의 규칙을 넣지 않는다). 셸 `result()` 점수(정수, 클수록 좋음): 생존자 `100 + (5 - misses) * 10`, 탈락자 `eliminatedRound * 10`(1~5라운드 → 10~50, 늦게 탈락할수록 높음). 생존자는 항상 탈락자보다 위. 같은 라운드 탈락자는 공동. `text`에는 "HP n/5 · 피격 m회"를 표시. `lowerIsBetter` 없음.
- **원작 점수 예시**: 5라운드 종료 결과 예 — 1P 5/5 HP(0 miss) 1위, 2P 3/5(2 miss) 2위, COM 1/5 3위, COM 탈락(5 miss, R4에서 탈락) 4위.

## 5. 난이도 곡선

- **라운드 길이**: 4→6→8→10→12 [확정]. 인간 기억 한계(7±2)를 R3부터 넘김 — 이것이 "가장 어려운 미니게임"인 이유.
- **표시 속도**: 불명 — 추정 0.60/0.55/0.50/0.45/0.40s 개당(노래 템포가 조금씩 빨라지는 느낌).
- **입력 제한시간**: 위 `1.0s×n + 2s`. 5라운드 모두 동일 비율.
- **COM 난이도** (원작 Easy/Normal/Hard + 비밀 Hyper [확정]; Very Hard는 스타디움 2 명칭 — 셸 호환 위해 5단계 정의):
  - 핵심 변수는 **한 라운드에서 순서를 정확히 재현할 확률**과 **입력 속도**. Easy는 R3 이후 거의 항상 실수, Hyper는 거의 완벽.
  - 수치는 §8.

## 6. 화면 구성

- **카메라**: 정면(교실 앞에서 학생들을 바라보는 시점), 약간 위에서 내려다봄. 횡스크롤/탑다운 아님.
- **배치 (960×540 기준)**:
  - 칠판: 상단 중앙 `x 200~760, y 40~200`. 화살표는 칠판 안에 좌→우로 한 줄(12개까지, 폭 44px 간격), 10개 넘으면 두 줄.
  - 선생님: 칠판 왼쪽 옆 `x≈130, y≈230` (scale 1.6). 채점 때 각 학생 앞으로 걸어감.
  - 학생 4명: 하단 한 줄, `x = 180, 380, 580, 780`, `y = 400`, scale 1.0. 각각 앞에 작은 책상(갈색 rr).
  - HUD: 각 학생 머리 위 `drawPlayerTag`(1P/2P CPU…), 그 바로 아래(y≈330)에 **피격 마크 5칸**(Serebii: "marks errors above each Clefairy's head" — 원작은 머리 위에 오답 표시가 쌓임). 빈 칸=흰 ○, 맞으면 빨간 ✕로 채움(왼쪽부터). 발밑 하트는 두지 않는다(정보 중복·원작과 다름). [확정(머리 위) + 추정(모양)] 입력 단계에 각 학생 위에 진행 점(●○○○: 입력한 개수/총 개수). 타임 바는 칠판 아래 `y 210` 가로 바.
  - 좌상단 "라운드 N / 5", 중앙 자막(메시지 1.2s).
- **배경/색감**: 크림색 벽(`palette.cream`), 나무 바닥, 창문 2개(하늘색), 초록 칠판, 분홍 톤 학생(플레이어 색). 전체적으로 파스텔 유치원 느낌. 입력 단계엔 벽을 살짝 분홍(`#FFE0EC`)으로 바꿔 긴장감.

## 7. 연출

- **시작**: 셸의 Ready?/Go! 이후 선생님이 지시봉으로 칠판을 두드리며 "잘 보세요~" 자막, `sfx.count`.
- **보여주기**: 화살표 1개씩 그려질 때 `sfx.tap` + 방향별 beep(§3), 선생님이 지시봉을 그 방향으로 휘두름, 칠판 화살표는 0.15s 동안 스케일 1.3→1 팝. 원작의 "Clefairy! Clefairy!" 노래는 beep 멜로디로 대체.
- **지우기**: 지우개 사각형이 좌→우로 스윕하며 화살표 페이드.
- **입력 성공(개별 키)**: 플라밍고가 방향 포즈로 0.25s, 발밑에 화살표 글리프 잔상 0.3s.
- **라운드 채점**: 
  - 오답자: 선생님이 0.3s 만에 앞으로 이동 → 망치 내려침 `sfx.hit` → 학생 `hit` 0.3s 후 `dizzy` 0.6s, 머리 위 ★ 파티클, 하트 하나가 회색으로 깨짐(`sfx.bad`). 텍스트 "땡!"
  - 정답자: `fly` 0.6s + "정답!" 텍스트, `sfx.good`.
  - 탈락: 하트 0 → `sleep`, "탈락…" 자막, `sfx.lose`.
- **종료**: 최후 1인 또는 R5 후 → 선생님이 손뼉, 승자 `fly` 반복 1.2s, `sfx.win` → `g.finish()`.
- **아나운서 대사 요지**(원작 Kids Club 아나운서, 자막으로 대체): "선생님이 그리는 순서를 잘 외우세요!" / "이제 따라 해 보세요!" / "아이고, 맞았네요!" / "마지막 라운드! 12개나 됩니다!" / "승자는 ○P!"

## 8. AI 행동 모델 (COM)

라운드 시작 시 COM별로 "이번 라운드 성공 여부"를 확률로 굴리고, 실패라면 몇 번째에서 틀릴지(균등 1..n) 정한다. 입력은 `erase` 끝 뒤 `reaction` 지연 후 `perKey` 간격으로 한 개씩 넣는다.

| 난이도 | 반응 지연 reaction(ms) | 키 간격 perKey(ms) | 개당 정확도 p_key | 라운드 성공률 (n=4/6/8/10/12) | 무입력(시간초과) 확률 |
|---|---|---|---|---|---|
| Easy | 900±300 | 700±200 | 0.85 | 0.52/0.38/0.27/0.20/0.14 | 8% |
| Normal | 600±200 | 500±150 | 0.92 | 0.72/0.61/0.51/0.43/0.37 | 3% |
| Hard | 400±150 | 380±100 | 0.95 | 0.81/0.74/0.66/0.60/0.54 | 1% |
| Very Hard(셸 호환용, 원작 없음) | 300±100 | 300±80 | 0.975 | 0.90/0.86/0.82/0.78/0.74 | 0% |
| Hyper | 200±50 | 220±50 | 0.99 | 0.96/0.94/0.92/0.90/0.89 | 0% |

- 라운드 성공률 = p_key^n. 라운드가 길수록 자동으로 실패가 늘어 원작의 "후반에 COM도 우수수 맞는" 느낌을 낸다. [미확인 — 2차 검증에서 GamesRadar 원문에 이 문장이 없었음(검색 요약의 환각 가능성). 다만 StrategyWiki "hardest of the mini-games"와 12개 기억이라는 과제 특성상 COM도 후반에 자주 틀리는 편이 자연스러워 하향치를 유지] → 이 게임은 원작에서도 COM이 약한 편이라 Hard까지는 12개 라운드 성공률을 0.6 아래로 잡았다(초판 표는 Hard 0.74로 지나치게 강했음 — 하향 조정).
- 원작은 COM 3명이 **모두 같은 난이도**(Kids Club 설정값). 셸에 난이도 선택이 없으면 **3명 모두 Normal**로 두고 COM별 p_key에 ±0.01 개체차만 준다(섞어 배정하지 않음). 사람 수가 4명이면 COM 없음.
- 실수 위치를 시각화: 틀리는 키에서 플라밍고가 엉뚱한 방향 포즈를 취한다(플레이어가 눈치챌 수 있음).

## 9. 초보자 설명 · 제목

- **한 줄 설명**: "선생님이 칠판에 그린 화살표 순서를 외웠다가 방향키로 똑같이 따라 하세요. 틀리면 뿅망치, 5대면 탈락!"
- **제목안**: **플라밍고 세이즈**(현행 유지, 7자). 대안: "따라해 밍고".

## 10. 출처와 신뢰도, 불명 항목

| 출처 | 얻은 정보 | 신뢰도 |
|---|---|---|
| https://bulbapedia.bulbagarden.net/wiki/Pok%C3%A9mon_Stadium_series_mini-games | 규칙 원문(망치, HP, 5대 탈락, 5라운드, 최소 miss 승리, Control Pad), COM 난이도 Easy/Normal/Hard/Hyper | 0.95 |
| https://bulbapedia.bulbagarden.net/wiki/Clefairy_Says | 위와 동일 요약 | 0.9 |
| https://www.koopatv.org/2023/06/pokemon-stadium-kids-club-minigame-tier.html | 라운드별 화살표 수 4/6/8/10/12, 노래, 치팅 가능(누적 표시) | 0.8 |
| https://strategywiki.org/wiki/Pok%C3%A9mon_Stadium/Mini-Games (2차 검증에서 원문 직접 확인) | "remember the arrow pattern carefully, then repeat it in a limited time. The Clefairy will then dance the moves; if they make an error, the teacher will bop them with a mallet. The last Clefairy to stay in the game without getting bopped five times is the winner; alternatively, the Clefairy with the least bops at the end of the game is the winner. This is also the hardest of the mini-games." | 0.9 |
| https://screenrant.com/pokemon-stadium-every-minigame-ranked-worst-best/ | 교실 배경, 노래하며 춤 | 0.6 |
| https://www.cbr.com/best-mini-games-from-pokemon-stadium-1-2-ranked/ | 칠판에 그린 뒤 지워짐, 실패 시 HP 감소 | 0.6 |
| https://www.serebii.net/stadium/kidsclub.shtml | "blackboard in the back of the room", "press the directions in order on the D-Pad", "fewest error marks wins", Who's the Best 모드, Hyper=Hard 5연승 | 0.85 |
| https://www.gamesradar.com/pokemon-stadium/cheats/ (2차 검증에서 원문 직접 확인) | 원문은 "KIDS CLUB DIFFICULTY:HYPER — If you choose 'Who's The Best' and beat the CPU 5 times in a row you will unlock a new difficulty option: 'Hyper'" 뿐. 초판이 인용한 "or tie"·"computers are notably weak" 문장은 **원문에 없음** → 해당 두 항목은 추정으로 강등 | 0.8(원문) / 0.3(초판 인용) |
| 접근 실패(2차도 동일): GameFAQs 가이드(7475), pokemon.fandom, nintendo.fandom, 일본어 ポケモンWiki(ピッピのいうとおり), IGN, TV Tropes, Reddit | — | — |

**불명 항목(추정으로 채움)**: 화살표 개당 표시 시간, 입력 제한 시간(초), 라운드당 최대 피격 수(1대 가정), 보여주기 중 선입력 허용 여부, 입력 중 즉시 정오 피드백 여부, 동점 처리, 컨트롤 스틱 동작, COM의 실제 성공률, 정확한 화면 좌표/카메라 각도, 아나운서 정확한 대사.

## 비평가 보강 (2026-09-09, 2차 출처 대조: Bulbapedia 2페이지·Serebii·KoopaTV·GamesRadar/StrategyWiki 검색 요약)

원작과 대조해 **틀린 것은 본문에서 고쳤고**, 빠진 것은 아래에 추가한다. 각 항목 끝에 [확정]/[추정] 표기.

1. **동점 규칙 수정** (§4): 원작 Kids Club에는 무승부가 존재한다("win or tie"). 동일 misses는 공동 순위, 타이브레이크 없음. `result()` 점수식을 생존자 `100+(5-misses)*10`, 탈락자 `탈락라운드*10`으로 확정해 셸 순위가 원작(생존자 > 탈락자, 늦게 탈락할수록 상위)과 같게 했다. [확정+설계]
2. **COM 배정 수정** (§8): 원작은 COM 3명 전원이 같은 난이도. "Normal/Hard/Very Hard 혼합"은 원작에 없으므로 삭제, 기본 전원 Normal. [확정]
3. **COM 강도 하향** (§8 표): 원작은 Hard에서도 "COM이 눈에 띄게 약한" 미니게임. p_key를 Easy 0.85 / Normal 0.92 / Hard 0.95 / Hyper 0.99로 재보정해 12개 라운드에서 Hard COM 성공률이 ~0.54가 되게 했다. 사람이 외우기만 잘하면 이기는 원작 체감을 유지. [확정(정성) + 추정(수치)]
4. **점수 단위 명칭**: Bulbapedia는 "Hit Points", Serebii는 "error marks"(오답 표시). 화면 HUD는 하트 5개(HP)로 두되, 결과 텍스트에 "피격 m회"를 함께 써서 두 표현을 모두 만족. [확정]
5. **특수 아이템·장애물 없음**: 전 출처 어디에도 아이템/보너스/함정 언급 없음. 라운드마다 정해진 개수의 화살표만 나온다. 구현에 아이템 시스템을 넣지 말 것. [확정(부재)]
6. **화살표 생성 규칙**: 4방향 균등 무작위, **연속 중복 허용**(↑↑↓ 가능). 원작도 같은 방향이 이어지는 패턴이 흔하고 이것이 기억을 어렵게 만드는 요소. 정렬이나 중복 방지 처리를 넣지 말 것. [추정(원작 영상 체감)]
7. **입력 단계 조기 종료** (§3): 생존자 전원 완료 시 0.8s 뒤 채점으로. 원작의 빠른 라운드 회전 체감을 위해 필요. [추정]
8. **전체 타이머 없음 재확인**: 게임 전체 제한 시간 없음, 라운드 입력 단계에만 제한 시간("repeat it in a limited time" — StrategyWiki). `duration: 0` + `g.finish()`. [확정]
9. **종료 조건 재확인**: (a) 생존자 ≤1 즉시 종료 — 단, 마지막 라운드 채점에서 **동시에 여러 명이 5대째**를 맞아 생존자 0명이 되면 "그 라운드까지의 misses 최소"로 순위(모두 5이면 탈락 라운드 기준 공동). (b) R5 채점 후 종료. [확정 + 예외는 설계]
10. **버튼 정리**: `up down left right` = 화살표 1개 입력(입력 단계에서만). `a`/`b` = 아무 기능 없음(원작 Control Pad 전용). 셸 타이틀/결과 화면의 A 버튼은 셸이 처리. 원작에 "취소/되돌리기" 없음 — 한 번 누른 화살표는 되돌릴 수 없다. [확정]
11. **선입력 불허 근거 보강**: 원작 팁이 "칠판을 보고 종이에 적어 치팅"을 언급할 뿐 "미리 입력"을 언급하는 출처는 없음 → `show`/`erase` 중 입력 무시 유지. 단, 입력 무시가 아니라 **큐에 쌓지 않는다**는 뜻(입력 단계 첫 프레임에 `hit`가 잔류하지 않도록 단계 전환 시 무시). [추정]
12. **화면 배치 보강** (§6): Serebii "칠판은 교실 뒤쪽(back of the room)" = 학생 뒤편 벽. 즉 카메라는 교실 앞에서 학생 정면을 보고, 칠판은 학생 머리 위 벽에 있다 — 본문 배치(칠판 상단 중앙, 학생 하단 한 줄)와 일치. 학생 순서는 왼쪽부터 1P·2P·3P·4P 고정. 선생님은 칠판 곁(왼쪽)에 서 있다가 채점 때만 학생 줄 앞을 좌→우로 이동하며 오답자만 때린다(1P→4P 순, 정답자 앞은 그냥 지나감). [확정(칠판 위치) + 추정(선생 동선)]
13. **"느낌" 보강** (§7): 
    - 원작의 상징은 **삐걱거리는 뿅망치 소리**("squeaky hammer")와 선생님의 화살표 노래. 망치 타격음은 `sfx.hit` 뒤에 `sfx.beep(1200→400Hz 하강 스윕 0.15s, 'square', 0.4)`를 겹쳐 "삐용" 소리를 만든다. 
    - 보여주기는 노래 박자에 맞춰 **등간격**으로 그린다(랜덤 지터 금지). 방향별 beep 음정(§3)을 4음 멜로디로 쓰면 원작의 "Cle-fai-ry" 챈트 대체가 된다.
    - 라운드 5 시작 때 "마지막! 12개!" 자막과 함께 `sfx.count` — 원작의 "가장 어려운 미니게임" 긴장감.
    - 채점 대기 중 정답자는 살짝 두리번거리는 `idle`, 맞은 직후 `dizzy`에 별 3개 회전 — "맞는 애니메이션이 웃기다"는 리뷰 어구를 살릴 것.
14. **난이도 곡선 재확인**: 사람 쪽 난이도는 오직 화살표 개수(4→12)와 표시 속도로만 상승. 입력 제한시간 비율(1s×n+2s)은 라운드 간 동일. 원작 난이도 설정(Easy~Hyper)은 **COM 실력에만** 영향을 주고 화살표 수·속도는 바꾸지 않는다. [확정(개수) + 추정(속도·시간)]
15. **기존 `games/says.html`과의 차이 재확인** (재작성 필수 항목): 1인 혼자 플레이(라이벌이 무작위로 탈락)·8라운드(3+round)·오답 즉시 탈락·`input.hit('ArrowUp')` 키코드 직접 사용·HP 없음 — 모두 원작과 다름. 새 구현은 4슬롯 `g.p(i).hit('up')`, 5라운드 4/6/8/10/12, HP 5, 라운드 말 일괄 채점으로.

### 남은 불명 항목 (추정값으로 구현, 나중에 영상 검증 권장)
- 화살표 개당 표시 시간(0.6→0.4s), 입력 제한 시간(1s×n+2s), 라운드당 최대 피격 1대, 입력 중 즉시 정오 피드백 없음, 아나운서 정확한 대사. 이들은 게임 규칙에 영향이 없고 체감 템포에만 영향을 주므로 위 수치로 구현해도 원작과 "다른 게임"이 되지는 않는다.

## 비평가 보강 (2차, 2026-09-10 — 독립 재검증: Bulbapedia 2페이지·StrategyWiki 원문·Serebii·KoopaTV·GamesRadar 원문·Wikipedia)

원문을 직접 다시 읽고 초판·1차 보강을 대조했다. **규칙 골격(4인 동시, 5라운드, 4/6/8/10/12개, Control Pad 입력, 오답·무입력 시 망치 1대, 5대 탈락, 최후 생존 또는 최소 피격 승리, 게임 전체 타이머 없음·입력 단계만 제한 시간, 화살표 누적 표시, 아이템·장애물 없음)은 전부 재확인**됐다. 아래는 고친 것과 새로 채운 것.

1. **[수정] 1차 보강의 잘못된 인용 2건 강등** — GamesRadar 원문에는 "win or tie"도 "computers are notably weak"도 없다(검색 요약이 만든 문장). §4 동점 규칙과 §8 COM 하향의 근거를 [추정]으로 바꿨다. 결론(동일 misses 공동 순위, COM 후반 약세)은 그대로 두되 근거 표기를 정직하게 고침. 일본 위키·GameFAQs는 2차에서도 접근 불가.
2. **[수정] 피격 표시 위치** — Serebii는 오답 표시가 "각 삐삐 **머리 위**"에 쌓인다고 명시. 발밑 하트 → 머리 위 ○/✕ 5칸으로 §1 표와 §6 HUD를 고쳤다. 플레이어 태그 바로 아래에 붙여 "누가 몇 대 맞았는지"가 항상 보이게 한다(계약: 각 플레이어 상태 상시 표시). [확정(위치)]
3. **[추가] 채점 단계의 '춤 재현'** — StrategyWiki: "The Clefairy will then dance the moves; if they make an error, the teacher will bop them." 입력이 끝난 뒤 학생이 자기가 넣은 순서를 춤으로 재현하고 그 뒤 채점된다. 구현: `judge` 첫 0.15s×n 동안 4명이 **동시에** 자기 입력을 순서대로 포즈로 재생(머리 위에 입력한 화살표를 작은 글리프로 하나씩 표시, 틀린 위치는 빨간색). 그 다음 선생님이 1P→4P로 이동하며 오답자만 망치. 이렇게 하면 "어디서 틀렸는지"가 보이고 원작의 춤 연출이 산다. 재현 시간은 0.15s×12=1.8s를 넘지 않게 고정 간격. [확정(춤·채점 순서) + 추정(간격)]
4. **[해결] 즉시 정오 피드백 없음** — 위 원문 구조(입력 → 춤 → 오답이면 망치)상 채점은 입력 이후에 일어난다. 입력 중에는 ✕/○를 띄우지 않고, 입력한 화살표도 화면에 남기지 않는다(외운 것을 확인할 수단을 주면 난이도가 원작보다 떨어짐). [확정(구조) + 추정(표시 억제)]
5. **[해결] 라운드당 최대 피격 1대** — 모든 출처가 "틀리면 망치로 때린다(hit it / bop them)"를 라운드 단위 단수 사건으로 서술하며 "틀린 개수만큼"이라는 언급은 없다. 12개 중 3개 틀려도 1대. 5라운드 × 1대 = 최대 5대 = 정확히 탈락 한계와 일치하는 것도 방증(5라운드 전부 틀려야 탈락). [확정에 준함]
6. **[해결] 컨트롤 스틱** — 모든 출처가 Control Pad/D-Pad만 언급. 셸은 스틱·D패드를 같은 `up/down/left/right` 액션으로 준다. `axis()`는 사용 금지, `hit()`만 사용. 대각 입력은 같은 프레임 두 액션 → up,down,left,right 우선순위로 1개만. [확정(원작 D패드) + 설계]
7. **[추가] 라운드별 확정 타임라인** (구현 상수표, 추정치 종합):

   | 라운드 | n | show 간격 | show 길이 | 입력 제한 | 최대 라운드 길이(intro 0.8+show+erase 0.6+입력+judge≤3.2+재현) |
   |---|---|---|---|---|---|
   | 1 | 4 | 0.60s | 2.4s | 6s | ≈13.6s |
   | 2 | 6 | 0.55s | 3.3s | 8s | ≈16.8s |
   | 3 | 8 | 0.50s | 4.0s | 10s | ≈19.8s |
   | 4 | 10 | 0.45s | 4.5s | 12s | ≈22.6s |
   | 5 | 12 | 0.40s | 4.8s | 14s | ≈25.2s |

   합계 상한 ≈98s, 전원 조기 완료 시 60s대. `duration: 0`, 라운드 상태기계 `intro→show→erase→input→replay→judge→(next|end)`.
8. **[추가] COM 입력이 제한 시간 안에 끝나도록 보정** — Easy 최악치(반응 1.2s + 12×0.9s = 12.0s)는 14s 안이지만, 구현 시 `perKey`를 `(limit − reaction − 0.5) / n` 이하로 클램프해 "느려서 시간초과"는 §8 표의 무입력 확률로만 발생하게 한다. COM 무입력(시간초과) 판정 시에는 아예 첫 입력을 하지 않는다(중간에 멈추지 않음). [설계]
9. **[추가] 화살표 순서 생성** — 라운드 시작마다 `Math.random`(셸 `rand`)으로 4방향 균등 독립 추출, 4명이 **동일 순서**를 본다(별도 시드 불필요: 한 번 생성해 공유). 라운드 간 이어붙이기 없음(원작은 라운드마다 새 패턴). [확정(공유·개수) + 추정(이어붙이기 없음: KoopaTV가 "four, four+two…"라고 개수만 말하고 앞 패턴 유지를 언급하지 않음)]
10. **[추가] 탈락자 처리** — 탈락자는 다음 라운드부터 `show`는 그대로 보되 입력 불가, 자리에서 `sleep`. 채점 시 선생님은 탈락자 앞을 건너뛴다. 결과 점수는 §4 식. [설계]
11. **[추가] 사람 전원 탈락 시** — 생존자가 COM만 2명 이상이면 게임은 규칙대로 계속되지만 지루하므로 남은 라운드를 **2배속**(show 간격·입력 제한 ½)으로 돌린다. 규칙엔 영향 없음. [설계]
12. **[추가] 셸 `howto`** — ["선생님이 칠판에 그리는 화살표 순서를 외우세요", "칠판이 지워지면 ↑↓←→로 순서대로 입력", "틀리거나 안 누르면 뿅망치 한 대, 5대면 탈락", "5라운드 뒤 가장 적게 맞은 사람이 승리"]. A/B 버튼 안내는 넣지 않는다.
13. **[재확인] 아이템·장애물·보너스 없음, 화면 문구 전부 한국어, 제목 "플라밍고 세이즈" 유지.**

### 남은 불명 항목(2차 후)
- 개당 표시 시간·입력 제한 시간·재현 간격의 **정확한 초 단위**(위 표는 추정), 아나운서 정확 대사, 동점 시 원작의 정확한 처리. 규칙·승패에는 영향이 없고 템포에만 영향 → 위 표대로 구현하면 원작과 다른 게임이 되지는 않는다. 사양은 이 상태로 구현 가능.
